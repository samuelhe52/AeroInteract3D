\documentclass[UTF8,a4paper,12pt,fontset=none]{ctexart}
\usepackage{ctreport}
\usepackage{amsmath,amssymb}
\usepackage{longtable}
\usepackage{calc}
\usepackage{etoolbox}
\usepackage{bookmark}
\IfFileExists{xurl.sty}{\usepackage{xurl}}{}
\IfFileExists{footnotehyper.sty}{\usepackage{footnotehyper}}{\usepackage{footnote}}
\makesavenoteenv{longtable}
\newcounter{none}

\makeatletter
\patchcmd\longtable{\par}{\if@noskipsec\mbox{}\fi\par}{}{}
\makeatother

$if(highlighting-macros)$
$highlighting-macros$
$endif$

\setlength{\emergencystretch}{3em}
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}
\urlstyle{same}

$for(header-includes)$
$header-includes$
$endfor$

$if(reportheader)$
\renewcommand{\ReportHeaderText}{$reportheader$}
$endif$
$if(title)$
\renewcommand{\ReportTitle}{$title$}
$endif$
$if(author)$
\renewcommand{\ReportAuthors}{$for(author)$$author$$sep$，$endfor$}
$endif$
$if(affiliation)$
\renewcommand{\ReportAffiliation}{$affiliation$}
$endif$
$if(authornote)$
\renewcommand{\ReportAuthorNotice}{$authornote$}
$endif$
$if(abstract)$
\renewcommand{\ReportAbstract}{$abstract$}
$endif$
$if(keywords)$
\renewcommand{\ReportKeywords}{$for(keywords)$$keywords$$sep$；$endfor$}
$endif$

\begin{document}
\makecomputingreporttitle

$if(toc)$
\clearpage
{
\setcounter{tocdepth}{$if(toc-depth)$$toc-depth$$else$3$endif$}
\tableofcontents
}
\clearpage
$endif$

$body$

$if(nocite-ids)$
\nocite{$for(nocite-ids)$$it$$sep$, $endfor$}
$endif$
$if(natbib)$
$if(bibliography)$
\reportbibliography{$for(bibliography)$$bibliography$$sep$,$endfor$}
$endif$
$endif$
\end{document}
