log_level: INFO
camera_index: 0
flip_camera: false
target_fps: 30
frame_width: 1280
frame_height: 720
render_position_sensitivity: 1.0
debug_stats: true
motion_preset: medium
aggressive_release_guard: false

# Virtual hand configuration
virtual_hand:
  # Coordinate scaling factor
  scale: 4.0
  # Depth scaling factor
  depth_scale: 4.0
  # How much near/far depth changes apparent hand size
  perspective_scale: 0.6
  # Debounce threshold (number of frames)
  stable_threshold: 3
  # Bone color (RGB, 0-1)
  bone_color: [1.0, 0.5, 0.0]  # Orange
  # Bone line width
  bone_width: 2.0
